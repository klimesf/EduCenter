{block content}
<h1 n:block="title">Přehled testu - {$test->name}</h1>
<h2>{!$test->desc}</h2>
<img src="{$basePath}/images/tests/{$test->img}" width="200" /><p>{!$test->about}</p>
<div class="spacer"></div>
<h2>Obsah testu</h2>
<p>
{foreach $settings as $setting}
<strong>{$setting->number_of_questions} otázek</strong> z kategorie <strong>{$setting->unit->name}</strong> s časovým <strong>limitem {$setting->time} minut.</strong><br />
{/foreach}
</p>
<div class="spacer"></div>
<p class="center"><a n:href="Test:run $test->id" class="button">Spustit test</a></div>
{/block}